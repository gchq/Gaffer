<settings>
    <servers>
        <server>
            <id>ossrh</id>
            <username>${env.OSS_NEXUS_USERNAME}</username>
            <password>${env.OSS_NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>ossrh-snapshots</id>
            <username>${env.OSS_NEXUS_USERNAME}</username>
            <password>${env.OSS_NEXUS_PASSWORD}</password>
        </server>
    </servers>

    <profiles>
        <profile>
            <id>ossrh</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <gpg.executable>gpg</gpg.executable>
                <gpg.keyname>${env.GPG_KEY_NAME}</gpg.keyname>
                <gpg.passphrase>${env.GPG_PASSPHRASE}</gpg.passphrase>
            </properties>

        </profile>
    </profiles>
</settings>
